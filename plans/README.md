# Implementation Plans

Generated by the improve skill on 2026-08-26. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done (unless your reviewer
maintains the index).

**Maintainer decision recorded 2026-08-26**: tests are deferred ("don't worry
about tests for now"). Plans contain NO test-writing steps; verification gates are
`npm run lint` + `npm run typecheck` + `npm run build` plus targeted greps. When
tests are introduced later, start with `plans/004` (sanitizeProject), `plans/007`
(history coalescing), and `plans/008` (resolveColorInto) — they were written to be
the highest-value future test targets.

**Note on "Planned at"**: the repo had no VCS when these plans were written
(plan 001 creates it). Drift checks are excerpt-based: compare the "Current state"
excerpts against live code before executing.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Git init + initial commit | P1 | S | — | TODO |
| 002 | Housekeeping (package name, dead assets, seed errors) | P3 | S | 001 | TODO |
| 003 | Tooling baseline (typecheck script, CI, AGENTS.md) | P1 | S | 001 | TODO |
| 004 | Sanitize project JSON + ErrorBoundary | P1 | M | 001 (003 recommended) | TODO |
| 005 | Agent tool input hardening | P1 | S–M | 001 (003 recommended) | TODO |
| 006 | Store correctness batch (targeting, rotate_90, tile refs, deleteFrame) | P1 | S | 001 (003 recommended) | TODO |
| 007 | Stroke coalescing + pointer robustness + tilemap redraw | P2 | M | 001 (003 recommended) | TODO |
| 008 | Color resolution consolidation + atomic palette adds | P2 | M | 005 | TODO |
| 009 | Strict TypeScript | P3 | M | 003; after 004–008 | TODO |
| 010 | `ensure_tilemap` agent tool | P2 | S | 001, 003 | TODO |
| 011 | `flood_fill`, `delete_frame`, `rename_sprite` tools | P2 | S | 001, 003, 005 | TODO |
| 012 | Human-visible Critique tab | P2 | S–M | 001, 003 | TODO |
| 013 | Share permalink + `import_project` tool | P3 | M | **004 (hard)** | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 is the root: every other plan needs git to exist (branching, diff review).
- 003's gates (`npm run typecheck`) are referenced by later plans; execute before
  004–013 when possible.
- 008 builds directly on 005's range-check semantics — do not reorder.
- 009 (strict TS) deliberately lands after 004–008 so those refactors don't need
  strict-cleanups twice.
- 013 hard-depends on 004's `loadProject` result object and sanitizer.
- 010 and 011 both touch the tool registry and README counts; execute sequentially
  (either order), reconciling the README count as you go.

## Findings considered and rejected

- Splitting `registerTools.ts` / `projectStore.ts` into modules: single-file
  registry is the app's stated showcase; the real harm (duplicated color policy)
  is fixed by plan 008 without a split.
- PWA/offline install: not grounded in repo evidence — noise.
- `get_tilemap` base36 legend collision at >26 distinct tiles: deferred; noted in
  plan 010's file as a maintenance watch item. Not worth blocking on.
- ChatGPT in-app browser iframe permissions-policy messaging (custom error copy
  when `registerTool` rejects with NotAllowedError): deferred until the app is
  tested in the real ChatGPT embed environment; the existing catch already sets an
  error status.
- `bresenhamLine` NaN infinite-loop path: no reachable caller (no line tool).
- Docs audit: README verified accurate; no findings.
