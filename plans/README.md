# Implementation Plans

Generated by the improve skill on 2026-08-26. The original plans were written for
an advisor-reviewed stack (`advisor/001-*` → `advisor/013-*`) based on root commit
`78aad52`. **The current `main` branch still points at that initial commit, while
the current UI/room implementation is an uncommitted working-tree change.**
Preserve that working tree in a real branch/commit before dispatching an executor;
the SHA drift check alone cannot see unstaged changes.

**Maintainer decision recorded 2026-08-26**: tests are deferred ("don't worry
about tests for now"). Plans contain NO test-writing steps; verification gates were
`npm run lint` + `npm run typecheck` + `npm run build` plus targeted greps. When
tests are introduced later, start with `plans/004` (sanitizeProject), `plans/007`
(history coalescing), and `plans/008` (resolveColorInto) — they were written to be
the highest-value future test targets.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Git init + initial commit | P1 | S | — | HISTORICAL DONE (`78aad52`) |
| 002 | Housekeeping (package name, dead assets, seed errors) | P3 | S | 001 | DONE (`2fc412d`) |
| 003 | Tooling baseline (typecheck script, CI, AGENTS.md) | P1 | S | 001 | DONE (`6a17d96`) |
| 004 | Sanitize project JSON + ErrorBoundary | P1 | M | 001, 003 | TODO; reapply to current checkout |
| 005 | Agent tool input hardening | P1 | S–M | 001, 003 | TODO; reapply to current checkout |
| 006 | Store correctness batch | P1 | S | 001, 003 | TODO; reapply to current checkout |
| 007 | Stroke coalescing + pointer robustness + tilemap redraw | P2 | M | 001, 003 | TODO; reapply to current checkout |
| 008 | Color resolution consolidation + atomic palette adds | P2 | M | 005 | TODO; reapply to current checkout |
| 009 | Strict TypeScript | P3 | M | 003; after 004–008 | HISTORICAL DONE; verify after reapply |
| 010 | `ensure_tilemap` agent tool | P2 | S | 001, 003 | TODO; reapply to current checkout |
| 011 | `flood_fill`, `delete_frame`, `rename_sprite` tools | P2 | S | 001, 003, 005 | TODO; reapply to current checkout |
| 012 | Human-visible Critique tab | P2 | S–M | 001, 003 | HISTORICAL DONE; present in current worktree |
| 013 | Share permalink + `import_project` tool | P3 | M | **004** | TODO; reapply to current checkout |
| 014 | Reliable room outbox | P1 | M | 005, 006, 007 | TODO |
| 015 | Conflict-free room palette merge | P1 | M | 005, 008, 014 | TODO |
| 016 | Bounded room cell-patch operations | P2 | L | 007, 014, 015 | TODO |
| 017 | Cute flat visual system | P2 | M | — | TODO |
| 018 | Game-engine handoff manifest and pack | P2 | M | 004, 013 | TODO |
| 019 | Room access policy and abuse guardrails | P1* | M | 014, 016 | TODO (required before public deployment) |

Historical final integration was verified on `advisor/013-share-permalink`: 22 tools
were exposed there (21 imperative + 1 declarative form). The current checkout
exposes 16 imperative tools plus the declarative form; do not treat the historical
tool count as current until plans 010, 011, and 013 are reapplied.

## Dependency notes

- 001 was the root (executed in-place; every later plan branched on it).
- 008 builds on 005's range-check semantics; 011 builds on 005/006/008; 013
  hard-depends on 004's result-returning `loadProject`.
- 014 should land after store/stroke behavior is stable. 015 must follow 008 so
  palette identity and capacity rules are shared. 016 is deliberately last among
  realtime changes because it changes the wire format and consumes 014/015's
  retry/conflict rules.
- 018 depends on the validated import path from 004/013. 019 is a deployment gate,
  not a prerequisite for local-only editing.

## Follow-ups (not plans, one-liners)

- `AGENTS.md`, `README.md`, and this historical index disagree about the tool
  count. The current source is authoritative at 16 imperative tools; reconcile the
  docs after plans 010/011/013 land.
- Known pre-existing, non-blocking warnings: oxlint `exhaustive-deps` on
  CanvasStage play interval (intentional deps design, plan 007) and CritiquePanel
  effect (plan 012); lightningcss notice for the WebMCP `:tool-form-active`
  pseudo-class (supported by Chrome; the CSS is preserved in the build).
- When tests are introduced: vitest + protocol/store/validation modules, plus a
  CI test job in `.github/workflows/ci.yml`.

## Findings considered and rejected

- Splitting `registerTools.ts` / `projectStore.ts` into modules: single-file
  registry is the app's stated showcase; the real harm (duplicated color policy)
  was fixed by plan 008 without a split.
- PWA/offline install: not grounded in repo evidence — noise.
- `get_tilemap` base36 legend collision at >26 distinct tiles: deferred (watch item).
- ChatGPT in-app browser iframe permissions-policy messaging: deferred until tested
  in the real ChatGPT embed environment.
- `bresenhamLine` NaN infinite-loop path: no reachable caller (no line tool).
- A full CRDT rewrite: not justified while the current room model can be made
  reliable with an outbox, identity-aware merges, and bounded patches.
- Automated tests in this pass: explicitly deferred by the maintainer decision;
  test targets remain recorded in each plan.
